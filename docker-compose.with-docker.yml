# OPTIONAL extension — user opts in after install (not for SoloHost package validation)
services:
  agent:
    image: ghcr.io/cannoi/pinode-telegram-solohost:v2.6.28
    labels:
      pi.ui.primary: "true"
    ports:
      - "127.0.0.1:18780:8080"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHAT_ID=${CHAT_ID}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - NODE_HOST=host.docker.internal
      - HORIZON_PORT=31401
      - CORE_HTTP_PORT=11626
      - DOCKER_PROBE=1
      - AUTO_DOCKER_SOCK=0
      - TELEMETRY_SEC=60
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - ./data:/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
