# SoloHost - NO docker.sock
# Build and push image v2.5.1 before install
services:
  agent:
    image: ghcr.io/cannoi/pinode-telegram-solohost:v2.5.1
    container_name: PiNode-Telegram-Controller
    labels:
      pi.ui.primary: "true"
    ports:
      - '127.0.0.1:18780:8080'
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHAT_ID=${CHAT_ID}
      - DATA_LIVE_URL=http://host.docker.internal:18790
      - NODE_HOST=host.docker.internal
      - HORIZON_PORT=31401
      - TELEMETRY_SEC=60
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - ./data:/data
    restart: unless-stopped
